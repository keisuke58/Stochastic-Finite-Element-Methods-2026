# Stochastic FEM — 基礎復習チートシート（口頭試問対策）

> **用途**：`oral_exam_qa.md` が「自分のプロジェクト防衛」用なのに対し、こちらは**講義#1〜#5の基礎理論**用。
> 試問では必ず「講義で扱った他の手法との関係」を聞かれる（例：「あなたはPCEを使ったが、KL展開は？MLMCは？」）。
> 講義資料 `SFEM_2026#1`〜`#5` に対応。

---

## 0. 全体地図（最初にこれを頭に描く）

不確実性を持つ問題 `L(u; θ) = f(θ)` を解く戦略は、大きく **3系統**：

| 系統 | 考え方 | 代表手法 | 侵入性 | 講義 |
|---|---|---|---|---|
| **① サンプリング** | 入力空間から点を撒いて決定論ソルバーを繰り返す | MC, QMC(Sobol), LHS, MLMC, MFMC | 非侵入 | #2, #4 |
| **② 選点法（Collocation）** | 賢く選んだ点で解き、応答を**補間**する | Lagrange補間 + 全テンソル/Smolyakスパースグリッド | 非侵入 | #2, #5 |
| **③ スペクトル展開** | 解を確率空間の**基底で展開**し係数を求める | PCE（非侵入=回帰/射影）、SSFEM（侵入=確率Galerkin） | 両方あり | #2, #3 |

**+ 補助系統**：摂動法（Perturbation, Taylor展開）、Neumann展開 — 小さいばらつき限定の古典手法。

**入力側の表現**（出力側の解法とは別軸）：確率変数 vs **確率場**（→ KL展開, 講義#3）。

> **試問の定番**：「あなたの手法はこの地図のどこにいるか」を即答できること。
> 自分＝**①のグリッド（Smolyak）で点を撒き、③のPCE基底に非侵入的に射影** ＝ ②と③のハイブリッド。

---

## 1. 確率論の基礎（講義#1 前半）

### 1.1 確率三つ組（probability triple）
`(Ω, 𝓕, ℙ)`
- **Ω**：標本空間（sample space）＝起こりうる全ての結果 ω の集合
- **𝓕**：σ-代数（事象の集合族）＝「確率を測れる」部分集合の族
- **ℙ**：確率測度 `ℙ: 𝓕 → [0,1]`、`ℙ(Ω)=1`

**確率変数（random variable）**：可測写像 `X: (Ω, 𝓕) → (ℝ, 𝓑(ℝ))`
＝「可測」がキモ。任意の区間の逆像が 𝓕 に属する ⇒ 確率が定義できる。

> **試問**：「なぜ measure theory から始めるのか？」→ 確率場・無限次元の展開（KLE, PCE）を厳密に扱うには
> 「確率変数の空間 L²(Ω)」を Hilbert 空間として扱う必要があり、その土台が測度論だから。

### 1.2 分布と積率（moments）
- CDF：`F_X(x) = ℙ(X ≤ x)`、PDF：`f_X(x) = dF_X/dx`
- **k次の積率**：`m_k = E[X^k] = ∫ x^k f(x) dx`
- **k次の中心積率**：`μ_k = E[(X-μ)^k]`

| 量 | 式 | 意味 |
|---|---|---|
| 平均 mean | `μ = E[X]` | 位置 |
| 分散 variance | `σ² = E[(X-μ)²] = E[X²] - μ²` | 広がり |
| 変動係数 CoV | `σ/μ` | 無次元のばらつき |
| 歪度 skewness | `γ₁ = μ₃/σ³` | 非対称性 |
| 尖度 kurtosis | `γ₂ = μ₄/σ⁴`（正規=3） | 裾の重さ |

### 1.3 多変数・相関
- 同時密度 `f_{XY}(x,y)`、周辺化 `f_X(x) = ∫ f_{XY} dy`
- **共分散** `Cov(X,Y) = E[(X-μ_X)(Y-μ_Y)]`、**相関係数** `ρ = Cov/(σ_X σ_Y) ∈ [-1,1]`
- 共分散行列 `𝐂 = E[(𝐗-𝛍)(𝐗-𝛍)ᵀ]`（対称・半正定値）

> **⚠️ 頻出の引っかけ**：「独立 ⇒ 無相関」は真、**「無相関 ⇒ 独立」は偽**。
> 例外は**同時ガウス分布**のときだけ（このときは無相関 ⇔ 独立）。
> 条件付き確率：`ℙ(A|B) = ℙ(A∩B)/ℙ(B)`、独立なら `ℙ(A∩B) = ℙ(A)ℙ(B)`。

### 1.4 不確実性の2分類（超頻出）

| | **アレアトリック**（偶然的） | **エピステミック**（認識論的） |
|---|---|---|
| 別名 | irreducible / 統計的 | reducible / 系統的 |
| 正体 | 物性の**固有のばらつき** | **知識不足** |
| 例 | 材料強度の個体差、荷重の変動 | モデル形式の誤り、パラメータ推定の不確かさ、データ不足 |
| データを増やすと | **減らない** | **減る** |
| 数学的扱い | 確率分布 | ベイズ、区間解析、p-box、evidence theory |

> **試問**：「あなたの5パラメータはどちら？」→ **アレアトリック**（製造ばらつき）として扱った。
> ただし平均値・CoVの推定自体は文献値で**エピステミック**な不確かさを含む。
> Topic 6 のベイズ更新はまさに**エピステミック不確実性の低減**（データで事後分布を絞る）。

---

## 2. FEM の復習（講義#1 後半）

確率化する前の**土台**。ここを聞かれても止まらないこと。

1. **強形式**（楕円型BVP）：`div σ + b = 0` in Ω、`u = ū` on Γ_D、`σ·n = t̄` on Γ_N
2. **弱形式**：試験関数 δu を掛けて部分積分（Gauss の発散定理）
   `∫_Ω σ : δε dΩ = ∫_Ω b·δu dΩ + ∫_Γ t̄·δu dΓ`
   → 微分の階数が下がり、C⁰ 連続な形状関数で足りる
3. **構成則**（Hooke）+ 形状関数 `u ≈ 𝐍 𝐮ᵉ`、`ε = 𝐁 𝐮ᵉ`
   → 要素剛性 `𝐊ᵉ = ∫ 𝐁ᵀ 𝐃 𝐁 dΩ`
4. **アセンブル** → `𝐊 𝐮 = 𝐟`
5. **非線形**：Taylor展開／Gateaux微分で線形化 → 接線剛性 `𝐊_T`
   → Newton–Raphson（荷重増分ループ + 平衡反復）
   非線形の分類：**材料非線形／幾何学的非線形（大変形）／境界非線形（接触）**

> **接続**：確率化すると `𝐊(θ) 𝐮(θ) = 𝐟(θ)` になる。
> - **非侵入**：θ をサンプルごとに固定して普通に解く（＝Abaqusをそのまま使える）
> - **侵入(SSFEM)**：𝐮 を PCE 展開して代入し、確率空間で Galerkin 射影 → 巨大な連立系

---

## 3. 確率場（Random Fields, 講義#1後半 + #3）★最重要★

### 3.1 定義
**確率場** `H(x, θ)`：空間座標 `x ∈ D` と標本 `θ ∈ Ω` の関数。
- x を固定 → **確率変数**
- θ を固定 → **1つの実現（realization）＝空間の関数**

分類：
- **1次元/n次元**：`x` の次元
- **1変量/多変量（univariate/multivariate）**：`H` 自体の次元
- **確率過程（stochastic process）**：`x` が時間のとき

**ガウス場**：任意の有限個の点の同時分布が多変量ガウス ⇒ **平均関数と共分散関数だけで完全に決まる**。

### 3.2 自己共分散と相関長
- **自己共分散関数**：`C(x₁,x₂) = E[(H(x₁)-μ(x₁))(H(x₂)-μ(x₂))]`
  → 2点間の**線形依存性**を表す
- **弱定常（weakly stationary / homogeneous）**：平均が一定、かつ `C` が**差 τ = x₁-x₂ のみ**に依存
  （強定常＝全ての有限次元分布が平行移動不変。弱定常はその2次までの版）
- **等方（isotropic）**：`C` が `|τ|` のみに依存（方向によらない）

**代表的な相関カーネル**（σ² は分散、`l_c` は**相関長**）：

| カーネル | 式 | 性質 |
|---|---|---|
| 指数型 exponential | `C(τ) = σ² exp(-\|τ\|/l_c)` | 原点で微分不可 ⇒ 実現が粗い、固有値減衰が**遅い** |
| ガウス型 squared-exp | `C(τ) = σ² exp(-(τ/l_c)²)` | 無限回微分可 ⇒ 実現が滑らか、固有値減衰が**速い** |
| 修正指数型 | 上記の変種 | 講義#3 Folie 24 |

> **相関長 `l_c` の意味**：それ以上離れると相関がほぼ消える距離。
> - `l_c → ∞`：場が完全相関 ＝ **単一の確率変数**に退化（← 自分のプロジェクトはこれ！）
> - `l_c → 0`：白色雑音、各点独立 ⇒ 表現に無限の項が必要
> - **`l_c` が短いほど KL展開に必要な項数が増える**（＝実効次元が上がる）← 超頻出

### 3.3 Wiener–Khintchine の定理
弱定常場では、**自己共分散関数とパワースペクトル密度がフーリエ変換対**：

```
S(ω) = (1/2π) ∫ C(τ) e^{-iωτ} dτ        C(τ) = ∫ S(ω) e^{iωτ} dω
```
`C(0) = σ² = ∫ S(ω)dω`（全パワー＝分散）。
→ これが**スペクトル表現法（SPRM）**で場を生成する根拠。

### 3.4 確率場の離散化：3つの流派

| 流派 | 手法 | 特徴 |
|---|---|---|
| **点離散化** | 中点法（midpoint）、節点法、積分点法、形状関数法 | 実装が簡単。要素サイズを `l_c` より十分小さく必要。分散を過大評価しがち |
| **平均化離散化** | 空間平均法（spatial averaging）、重み付き積分法 | 要素上で平均 ⇒ **分散が減る**（分散低減）。より現実的 |
| **級数展開** ★ | **KL展開（KLE）**、スペクトル表現法（SPRM）、PCE、OSE | 少数の確率変数に**縮約**できる。SFEMの主流 |

> **試問**：「なぜ級数展開が優れるか」→ 点離散化は要素数だけ確率変数が必要（次元爆発）。
> KLEは**主要モードだけ**で場を表現でき、確率次元を劇的に下げられる。

### 3.5 Karhunen–Loève 展開（KLE）★★★

```
H(x, θ) = μ(x) + Σ_{i=1}^{∞} √λ_i  φ_i(x)  ξ_i(θ)
                      ↑固有値  ↑固有関数  ↑無相関・平均0・分散1のRV
```

**固有値問題 = 第2種 Fredholm 積分方程式**：
```
∫_D  C(x₁, x₂) φ_i(x₂) dx₂ = λ_i φ_i(x₁)
```

**性質（覚える）**：
1. 固有関数は**正規直交**：`∫_D φ_i φ_j dx = δ_ij`
2. `ξ_i` は**無相関**、平均0・分散1。**ガウス場なら独立な標準正規** ⇐ ここが決定的に嬉しい
   （非ガウス場では無相関だが独立とは限らず、扱いが難しい）
3. **平均二乗誤差の意味で最適**：M項で打ち切ったとき、あらゆる級数展開の中で**誤差最小**
4. 分散の再現：`Var[H_M(x)] = Σ_{i=1}^M λ_i φ_i(x)²` ⇒ **打ち切ると必ず分散を過小評価**
5. 打ち切り誤差の指標：`ε = 1 - Σ_{i=1}^M λ_i / Σ_{i=1}^∞ λ_i`（通常 95〜99% で M を決める）

**解き方**：
- **解析解**：単純形状（1次元区間）＋指数型カーネルのみ（超越方程式の解）
- **Galerkin法**：`φ_i(x) ≈ Σ_k d_k^{(i)} N_k(x)` と展開 → 一般化固有値問題 `𝐂𝐃 = 𝐁𝐃𝚲`
- 実務は数値的に固有値分解（自分の `topic9_kl_expansion.py` はこれ）

**KLE の手順（講義#3 Folie 25）**：
A. 共分散関数を選ぶ → B. 固有値・固有関数を計算 → C. 打ち切り次数 M を決める → D. RV `ξ_i` を生成

> **KLE vs PCE（頻出の対比）**
> | | KLE | PCE |
> |---|---|---|
> | 何を展開 | **入力**の確率場 | **出力**（応答）や非ガウス入力 |
> | 必要な情報 | **共分散関数が既知**であること | 入力分布のみ |
> | 基底 | 共分散の固有関数（問題依存） | 直交多項式（Wiener–Askey で決まる） |
> | 得意 | ガウス場の最適な次元縮約 | 非ガウス、任意の応答 |
> | 併用 | **KLEで入力場を M 個の ξ に縮約 → その ξ に対して PCE** ← これが標準の組合せ |

---

## 4. 多項式カオス展開（PCE, 講義#2 + #3）

### 4.1 基本形
```
Y(θ) ≈ Σ_{|α| ≤ p} c_α Ψ_α(ξ(θ)) ,   Ψ_α(ξ) = Π_{i=1}^d ψ_{α_i}(ξ_i)
```
- `α` は多重指数（multi-index）、`Ψ_α` は入力測度に関して**直交**
- **項数**：`P = C(d+p, p) = (d+p)! / (d! p!)`
  - d=5, p=2 → **21** ／ d=5, p=3 → **56**
  - p に対しては多項式的、**d に対して組合せ的に爆発**（次元の呪い）

### 4.2 Wiener–Askey スキーム（基底は入力分布で決まる）

| 入力分布 | 直交多項式 | 台 |
|---|---|---|
| **ガウス** | **Hermite** | (-∞, ∞) |
| 一様 | Legendre | [a, b] |
| ガンマ／指数 | Laguerre | [0, ∞) |
| ベータ | Jacobi | [a, b] |
| ポアソン（離散） | Charlier | {0,1,2,…} |

**Hermite（確率論版, He_n）**：`He_0=1, He_1=ξ, He_2=ξ²-1, He_3=ξ³-3ξ, He_4=ξ⁴-6ξ²+3`
漸化式 `He_{n+1} = ξ He_n - n He_{n-1}`、`E[He_n²] = n!`

> **なぜ測度に合わせるか**：合わせると**指数的（スペクトル）収束**。ずらすと収束が落ちる。
> 元の Wiener (1938) の同次カオスは Hermite のみ。Xiu & Karniadakis (2002) が **generalized PC (gPC)** に拡張。

### 4.3 直交性がくれるもの（タダで手に入る統計量）
`E[Ψ_α Ψ_β] = δ_αβ E[Ψ_α²]` より：
- **平均**：`E[Y] = c_0`
- **分散**：`Var[Y] = Σ_{α≠0} c_α² E[Ψ_α²]`
- **Sobol 指数**：`S_i = Σ_{α ∈ 𝒜_i} c_α² E[Ψ_α²] / Var[Y]`
  （`𝒜_i` = 変数 i だけが非ゼロの多重指数集合）
  **全 Sobol 指数** `S_T^i` は i を含む全ての α を足す。`S_i ≈ S_T^i` なら**交互作用なし＝加法的**
- 出力PDFはPCEを大量サンプリング（コストほぼゼロ）して得る

### 4.4 係数の求め方
| 方法 | 式 | 備考 |
|---|---|---|
| **射影（projection）** | `c_α = E[Y Ψ_α]/E[Ψ_α²]` を求積で評価 | 求積点＝Gauss-Hermite / Smolyak |
| **回帰（regression）** | `min ‖𝚿𝐜 - 𝐘‖²` の最小二乗 | 点数 > 項数 で優決定。**LOO-CVで誤差評価できる** |
| **スパース回帰** | LAR / LASSO で有効項だけ残す | 高次元で有効 |

**打ち切りスキーム（truncation）**：
- 標準：全次数（total degree）`|α|₁ ≤ p`
- **双曲型（hyperbolic）**：`(Σ α_i^q)^{1/q} ≤ p`, q<1 ⇒ 交互作用項を削り項数を大幅減（講義#5 Folie 36）

### 4.5 侵入型：SSFEM（確率Galerkin, 講義#2 Folie 25-26）
`𝐮(θ) = Σ_j 𝐮_j Ψ_j(ξ)` を `𝐊(θ)𝐮(θ)=𝐟(θ)` に代入し、残差を各 Ψ_i に直交させる：
```
Σ_j  E[Ψ_i Ψ_j Ψ_k]  𝐊_k 𝐮_j = 𝐟_i
```
- 系のサイズ：`N_dof × P` の**巨大ブロック連立系**（特徴的なスパースパターン）
- 解法：ブロック Gauss–Seidel など
- **利点**：1回解くだけ／**欠点**：ソルバーの書き換えが必須（商用コードでは不可）＋特別な算術（PC同士の掛け算など）が必要

> **講義#2 Folie 28 の比較データ（穴あき円板）**：
> MC 500回=18650s／SSFEM 1回=34900s／Collocation-Full 64点=2570s／**Collocation-Sparse 65点=2700s**
> ⇒ **選点法が圧勝**。SSFEMは「1回」でもコストが巨大。この数字は引用できると強い。

---

## 5. サンプリング法（講義#4）

### 5.1 モンテカルロ（MC）
- 手順：乱数生成 → 決定論ソルバーを N 回 → 統計処理
- **収束**：標準誤差 `σ/√N` ⇒ **rate = N^{-1/2}**
- ★**最大の長所：収束率が確率次元 d に依存しない**（次元の呪いに強い）
- ★**最大の短所：遅い**。桁を1つ改善するのに100倍のサンプルが必要

**乱数生成**：
- **LCG（線形合同法）**：`x_{n+1} = (a x_n + c) mod m` — 周期が短い、格子構造が出る
- **Mersenne Twister** — バイト演算ベース、超長周期、広く使われる
- **シャッフル**で系列相関を壊す
- 非一様化：**逆変換法** `X = F⁻¹(U)`、棄却法、Box–Muller

### 5.2 準モンテカルロ（QMC）
- **低食い違い量（low-discrepancy）決定論列**：van der Corput → Halton, **Sobol**列
- 収束：`O((log N)^d / N)` ⇒ 低次元では **~N^{-1}** で MC より速い
- ★**注意**：`(log N)^d` が効くので**高次元では優位性が消える**（次元依存性あり）

### 5.3 ラテン超方格サンプリング（LHS）
- **層化抽出（stratified sampling）without replacement**
- 手順：① 各次元を等確率の N 区間に分割 ② 各区間から1点ずつ ③ 次元間をランダムに組み合わせる
- 組合せ数：N分割・M次元で `(N!)^{M-1}`
- **各1次元周辺分布の被覆が保証される**（MCのような偏りが出ない）
- MCより分散が小さい。ただし**事前にNを決める必要**がある（MCは追加サンプルを継ぎ足せる）

> **MC vs LHS の実務的トレードオフ（講義#4 Folie 23）**：
> MCは「サンプル数を事前に決めなくてよい／追加可能」、LHSは「少数で高精度だがNを固定」。

### 5.4 分散低減とMCの改良
| 手法 | アイデア |
|---|---|
| **制御変量（control variate）** | `Ŷ = Y + α(Z - E[Z])`、Z は平均既知の相関RV。最適 `α* = -Cov(Y,Z)/Var(Z)` ⇒ 分散が `(1-ρ²)` 倍に |
| **重点サンプリング** | 稀事象の領域を重点的に引き、重みで補正（小さい P_f に必須） |
| **MLMC（多レベルMC）** | `E[Q_L] = E[Q_0] + Σ_l E[Q_l - Q_{l-1}]`。**粗メッシュで大量、細メッシュで少量** ⇒ 総コスト激減 |
| **MFMC（多忠実度MC）** | 高忠実度モデル(FEM)と低忠実度モデル(サロゲート)を**相関とコストから最適配分**して組合せ |

**MFMCの流れ**：pre-analysis（少数サンプルで各モデルのコストと相関を測る）→ 予算ごとに最適な実行回数と係数を決定 → MSEでMCと比較。

---

## 6. 確率選点法（Stochastic Collocation, 講義#2 + #5）

### 6.1 考え方
「選点（collocation points）で決定論ソルバーを呼び、応答を**補間多項式**で再構成する」
＝ サンプリング法だが、点が**ランダムでなく最適配置**。

**Lagrange 補間**：
```
u(ξ) ≈ Σ_{j} u(ξ_j) L_j(ξ) ,   L_j(ξ_k) = δ_jk
```
補間なので**選点上では厳密**（← PCE回帰との違い。PCEは最小二乗で点上では厳密でない）。

### 6.2 全テンソル積 vs スパースグリッド
- **全テンソル積**：`(p+1)^d` 点 → d=5, p=2 で 3⁵=243、p=4 なら 5⁵=3125…**爆発**
- **Smolyak アルゴリズム**：低次の1次元則の**差分作用素の組合せ**
```
A(q,d) = Σ_{q-d+1 ≤ |i| ≤ q} (-1)^{q-|i|} · C(d-1, q-|i|) · (U^{i₁} ⊗ … ⊗ U^{i_d})
```
- **総次数（total degree）多項式空間**の精度を保ちつつ点数を大幅削減
- 点数は `O(N (log N)^{d-1})` オーダー ⇒ 次元の呪いを**緩和**（消滅はしない）

### 6.3 グリッドの種類（どれを選ぶか聞かれる）

| グリッド | ネスト性 | 特徴 |
|---|---|---|
| **Clenshaw–Curtis** | **ネスト** | Chebyshev点ベース。レベルを上げても既存点を再利用でき、適応化に向く |
| **Gauss–Hermite** | 非ネスト | ガウス測度に最適（n点で2n-1次まで厳密）だがレベル間で点を捨てる |
| **Gauss–Leja** | **ネスト** | 逐次的に点を追加できる |
| **Kronrod–Patterson (K-P)** | **ネスト** | **高次元・非線形問題で最も頑健**（講義#5 Folie 14, Dannberg） |

### 6.4 発展形
- **異方性 Smolyak（anisotropic）**：次元ごとに重要度の重みを変え、効く方向にだけ点を密に
  → **自分のケースなら E1 方向に重点配分できる**（S_E1=0.992 なので理想的な適用先）
- **適応スパースグリッド**：誤差指標で階層的に細分化。**特異点や非平滑応答**に対応（Folie 17）
- **応答の平滑性が命**：Lagrange/PCEとも滑らかさを前提。破壊分岐など不連続があると**Runge現象・振動**が出て破綻
  → 対策：適応/multi-element PCE、またはサンプリングに戻る

---

## 7. 古典的な摂動系（講義#2 Folie 18-20）

| 手法 | 内容 | 限界 |
|---|---|---|
| **摂動法（Perturbation）** | 平均まわりに Taylor 展開：`u ≈ u₀ + Σ (∂u/∂x_i)Δx_i + ½ΣΣ(∂²u/∂x_i∂x_j)…` → 期待値・分散を解析的に | **入力ばらつきが小さいときのみ**（CoV 目安 <10〜15%）。感度（微分）が必要 |
| **Neumann展開** | `(𝐊₀+Δ𝐊)⁻¹ = (𝐈 - 𝐊₀⁻¹Δ𝐊 + (𝐊₀⁻¹Δ𝐊)² - …)𝐊₀⁻¹` | MCと併用して**再分解を回避**（𝐊₀の分解を使い回す）。級数の収束条件あり |

> 自分の CoV は最大50%（Kn, GIc, tn）⇒ **摂動法は使えない**。これも「なぜPCEか」の理由になる。

---

## 8. 非線形材料と確率（講義#2 Folie 29-34）

- 延性損傷モデル等では**降伏関数・発展則がランダム**になる ⇒ **降伏曲面自体が確率的**
- Radial return mapping の中で確率量を扱う必要が出る
- 実務的な扱い：**平均値を使う**のが一般的だが、**分位点（quantile）を使う**方が安全側で合理的な場合がある
- 例：4点曲げでの損傷変数の平均・標準偏差の分布

> **接続**：自分のCZM（凝集域モデル）はまさに「非線形・損傷」だが **SDEG≡0** ＝損傷が発生せず、
> この難しさが顕在化しなかった。だから将来課題で「損傷を駆動する荷重」を挙げている。

## 9. 確率過程（講義#2 Folie 45-48, 発展）
- **Markov 性**（遷移確率）、Chapman–Kolmogorov 方程式
- **Fokker–Planck 方程式**：確率密度の時間発展 `∂p/∂t = -∂(A p)/∂x + ½∂²(B p)/∂x²`
  `A`=ドリフトベクトル、`B`=拡散行列。起源はブラウン運動
- 時間依存の確率問題（疲労、劣化）に繋がる

---

## 10. 手法の比較表（試問で一番効く一枚）

| 手法 | 収束 | 次元の呪い | 侵入性 | 感度(Sobol) | 適する場面 |
|---|---|---|---|---|---|
| **MC** | N^{-1/2} | **なし** ★ | 非侵入 | 追加計算が必要 | 高次元、非平滑、稀事象（+重点/subset） |
| **QMC** | ~(logN)^d/N | あり（高次元で劣化） | 非侵入 | 同上 | 中次元・滑らか |
| **LHS** | MCより良 | 弱い | 非侵入 | 同上 | 少数サンプルでの被覆 |
| **MLMC/MFMC** | コスト最適化 | なし | 非侵入 | 同上 | メッシュ階層／低忠実度モデルがある場合 |
| **Collocation(全テンソル)** | 指数的（滑らかなら） | **強い** (p+1)^d | 非侵入 | 補間から | 低次元(d≲3) |
| **Collocation(Smolyak)** ★ | 指数的 | **緩和** | 非侵入 | 補間/PCE化 | **中次元(d≲10-20)・滑らか** ←自分 |
| **PCE(非侵入)** ★ | 指数的 | 緩和（スパース化で） | 非侵入 | **解析的に無料** ★ | **滑らか・中次元・感度が欲しい** ←自分 |
| **SSFEM(侵入)** | 指数的 | 強い（P倍の系） | **侵入** | 解析的 | 自作コード、少数RV |
| **摂動法** | 2次まで | なし | 準侵入 | 微分から | **小さいばらつきのみ** |

> **「なぜ自分はSmolyak+PCEか」の1文回答**：
> 「d=5 の中次元、応答は滑らかでほぼ線形、商用ソルバー（Abaqus）で侵入不可、
> かつ**Sobol感度が主目的**。この4条件を全て満たすのは非侵入PCE + スパースグリッドだけ。」

---

## 11. 「講義↔自分のプロジェクト」対応表（必ず聞かれる橋渡し）

| 講義の話題 | 自分のプロジェクトでの扱い | 聞かれたときの一言 |
|---|---|---|
| 確率三つ組・RV | 5つの打ち切り正規RV | 正値性のため打ち切り（検査限界が根拠） |
| アレアトリック/エピステミック | 入力=アレアトリック、Topic6=エピステミック低減 | ベイズ更新で事後std 74%減 |
| **確率場・KLE** | 本体は `l_c → ∞`（完全相関＝1RV）と仮定。**Topic 9 でKLEを実施** | バッチ単位のばらつきが支配的。空間平均で実効剛性のstdが 7,300→**約4,700 MPa**。ただし**入力側の結果**（§15参照） |
| 相関長 | Topic 9 で異方性指数カーネル | `l_c` が短いほど固有値減衰が遅く、必要モード数増 |
| **Wiener–Askey** | ガウス入力 → **Hermite** | 基底は測度が決める、任意ではない |
| **Smolyak** | deg2で66点、deg3で286点 | 全テンソル 3⁵=243 → スパースで66点 |
| MC/QMC/LHS | サロゲート上で10⁶回MC（PDF・P_f評価） | 高価なFEMではなく**安いPCEの上で**MCを回す |
| MLMC/MFMC | 未使用 | ODBが293MB/回で階層を組むコストが見合わない。将来課題 |
| 摂動法 | 不採用 | CoVが最大50%で小変動の仮定を満たさない |
| SSFEM | 不採用 | Abaqusのソース改変が不可能 |
| 非線形・損傷 | CZM（BK則, η=2.284） | ただしSDEG≡0で損傷が発生せず |
| 適応/異方性 | 未使用 | S_E1=0.992 なので**異方性Smolyakが理想的な次の一手** |

---

## 12. よくある落とし穴（言ってはいけないこと）

| ❌ NGな発言 | ✅ 正しい言い方 |
|---|---|
| 「無相関だから独立」 | 「無相関。独立と同値なのは**同時ガウス**のときだけ」 |
| 「次数を上げれば精度が上がる」 | 「モーメント収束とLOO-CVで**最適次数を選ぶ**。過剰次数は過学習」 |
| 「P_f = 0 です」 | 「**解析の分解能以下**。MCは10⁻⁶が限界、入力も±50%で打ち切り済み」 |
| 「PCEはNNより優れている」 | 「**この問題では**。滑らかで低次という正しい事前知識を持つから」 |
| 「KL展開は分散を正確に再現する」 | 「打ち切ると**必ず分散を過小評価**する（`Σ_{i≤M} λ_i φ_i²`）」 |
| 「Smolyakで次元の呪いは解決」 | 「**緩和**。`O(N(logN)^{d-1})` でまだ次元依存。d≳20では厳しい」 |
| 「MCは遅いだけの手法」 | 「収束率が**次元に依存しない**唯一の手法。非平滑・高次元では最後の砦」 |
| 「Sobol指数の計算に追加解析が要る」 | 「PCEなら**係数の二乗和だけ**で解析的。追加実行ゼロ」 |

---

## 13. 30秒で答える定義集（直前チェック）

- **確率変数**：可測空間から実数への**可測写像** `X:(Ω,𝓕)→(ℝ,𝓑)`
- **確率場**：`H(x,θ)`。x固定でRV、θ固定で空間関数
- **弱定常**：平均一定 & 共分散が**差のみ**に依存
- **相関長 `l_c`**：相関が実質消える距離。∞なら1つのRVに退化
- **Wiener–Khintchine**：自己共分散 ⇔ スペクトル密度がフーリエ対
- **KL展開**：`μ + Σ√λ_i φ_i(x) ξ_i`。固有対はFredholm積分方程式から。**平均二乗最適**
- **PCE**：`Σ c_α Ψ_α(ξ)`。Ψは入力測度に直交。項数 `C(d+p,p)`
- **Wiener–Askey**：分布↔直交多項式の対応（ガウス↔Hermite）
- **Smolyak**：低次1次元則の差分の組合せで総次数空間の精度を保つスパース求積
- **Sobol指数 `S_i`**：`Var(E[Y|X_i])/Var(Y)`。分散のうち変数iが説明する割合
- **信頼性指標 β**：`β = -Φ⁻¹(P_f)`。限界状態までの標準偏差の個数
- **アレアトリック/エピステミック**：減らせない固有のばらつき／知識不足で減らせる
- **非侵入 vs 侵入**：ソルバーを黒箱として使う／方程式そのものを書き換える
- **LHS**：層化抽出without replacement。各1次元周辺分布の被覆を保証
- **MLMC**：粗メッシュで大量＋細メッシュで少量の補正和

---

## 14. 想定シナリオ（口頭試問の典型的な流れ）

1. **「あなたのプロジェクトを5分で」** → `oral_exam_qa.md` の冒頭の数値を軸に
2. **「なぜその手法を選んだ？」** → §10 の比較表の1文回答
3. **「講義で扱った○○は使わなかったのか？」** ← ここが本チートシートの主戦場
   - KLE → 「Topic 9 で実施済み。本体はl_c→∞の仮定」
   - MLMC/MFMC → 「ODBサイズの制約。将来課題」
   - SSFEM → 「商用ソルバーで侵入不可」
   - 摂動法 → 「CoV 50%で仮定を満たさない」
4. **「結果の物理的意味は？」** → E1支配、SDEG≡0の意味（§Q8, Q12）
5. **「限界と今後は？」** → 「応答が良性すぎる」を軸にロードマップ（Q20b）

> **答え方の原則**：①**まず結論を一文**、②**次に理由（できれば数式か数値）**、③**最後に限界を自分から言う**。
> 限界を先に自分で言うと、審査員はそれ以上突けない。これが一番効く。

---

---

## 15. Topic 9（KL展開）の想定質問 — 一番突かれやすい4問

### Q-A. 「本体は E₁ を単一の確率変数、Topic 9 は確率場。整合するのか？」
スカラーモデルは**確率場の `l_c → ∞`（完全相関）極限**であり、別モデルではなく**特殊ケース**。
同時硬化された1枚のパネルでは、支配的なばらつきはバッチ間（クーポン単位）であり、パネル内の
空間変動ではない ⇒ 完全相関が第一近似として妥当。Topic 9 はその仮定を**検証しに行った**もの。

### Q-B. 「Fredholm 積分方程式を Galerkin で解いていないが？」
**点選点（Nyström）離散化**を使った。共分散行列を一様格子上でサンプリングして直接固有値分解している。
- Galerkin の固有関数は **L² 直交**（`∫φᵢφⱼdx = δᵢⱼ`）、こちらは **ℓ² 直交**（`φᵢᵀφⱼ = δᵢⱼ`）
- **一様格子なら両者はセル面積という定数倍しか違わない** ⇒ 説明分散の**比**では約分されて消える
- `C = ΦΛΦᵀ` が厳密に成り立つので、生成した実現はサンプル共分散を正確に再現する
- 格子は 20×15。最短相関長 `l_cx=200 mm` に対し**約6点/相関長**で、目安（4〜5点）を満たす

### Q-C. 「『スカラーは保守側』は最大ミーゼス応力についても言えるのか？」★最重要★
**言えない。ここは限定して答える。**
- 厳密に示したのは `Var[Ē₁] < σ²_E1`、つまり**入力側**（実効パネル剛性のばらつきが減る）だけ
- **平均支配の応答**（全体たわみ・膜応答）なら、入力分散の過大評価 ⇒ 応答分散の過大評価 ⇒ **保守側**
- しかし**最大ミーゼス応力は極値統計**。場が不均一だと剛性勾配と応力再配分で**局所的にピークが上がりうる**。
  平均剛性のばらつきが減ることは、ピークが下がることを**意味しない**
- 結論：ピーク応力についての保守性を主張するには **KL実現をFEモデルに写像して流す**必要があり、
  本研究ではやっていない ⇒ **今後の課題**

> **答え方**：「入力側については厳密に示せています。ただしQoIがピーク応力＝極値なので、
> そこまで一般化はできません。そこはFE評価が要る今後の課題です」と**自分から線を引く**。

### Q-D. 「タイトルは Non-Gaussian だが実装はガウス。しかも本体は打ち切り正規では？」
- タイトルは誤り（修正済み）。実装は**ガウス場**
- 打ち切り正規との整合：E₁ は **CoV=5%** なので `[0.5μ, 1.5μ]` は **μ ± 10σ** に相当
  ⇒ 打ち切りは**事実上不活性**、純ガウスと数値的に区別不能
- ★**逆に注意**：凝集パラメータは **CoV=50%** なので同じ `[0.5μ, 1.5μ]` が **μ ± 1σ**。
  こちらは**約32%の確率質量を切り落とす非常に強い打ち切り**。
  結果に影響しないのは SDEG≡0 で出力を動かさないからであって、打ち切りが軽いからではない
  （聞かれたら正直にこう言う。これを自分から言えると強い）

---

**関連ファイル**：`oral_exam_qa.md`（プロジェクト防衛Q&A・和訳付き）／`report_paper.pdf`（本編）／`slides_sfem.pdf`（発表資料）
